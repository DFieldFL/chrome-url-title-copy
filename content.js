// Content script — intentionally minimal.
// The extension uses chrome.scripting.executeScript on-demand
// rather than always-on injection. This file is a placeholder
// required by some older Chromium builds but contains no active code.
